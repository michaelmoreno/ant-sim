
type SimulationOptions = {
    ants?: number;
}


class Simulation {

    constructor(assetPath: string, options?: SimulationOptions) {
        // this.loader = new Loader(assetPath)
    }

    start() {
        // this.loader.load()
    }
}

export default Simulation
